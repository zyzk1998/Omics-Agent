# 与 environment.yml / Dockerfile 中 conda 环境对齐；pyradiomics 由镜像内 pip 安装（见 Dockerfile）。
# drug-similarity：database_query 依赖 requests（与 micromamba 显式安装一致，供 pip-only 场景对照）。
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10.0
requests>=2.28.0
matplotlib>=3.7
scikit-learn>=1.3.0
SimpleITK>=2.3.0
pyradiomics>=3.0.1
