---
description: 超大 HTML 的 UTF-8 安全编辑、正则与纯 CSS 吸顶（避免 2025-03 事故重演）
globs: services/nginx/html/**/*.html
alwaysApply: false
---

# 超大前端 HTML（本仓库 `index.html`）硬性约束

## 1. 禁止用「整文件重写」式补丁破坏 UTF-8

**事故**：对上万行 `index.html` 使用会重写全文件的编辑方式后，多字节字符（中文、emoji）被损坏成 `??`，页面大面积乱码。

**规则**：

- 优先：`git restore` / `git checkout --` 从干净提交恢复，再**只改必要片段**。
- 对超大文件：**避免**依赖可能触发「整文件以错误编码回写」的编辑路径；需要插入小段内容时，用 **Python/脚本以 `encoding='utf-8'` 读写**，或 IDE 中「仅替换唯一锚点字符串」且事后用 `git diff` 确认**仅出现预期行数变化**（若 diff 达数千行，立即中止并恢复）。
- 修改后自检：文件头注释中文是否正常、`safeMarkedParse` 等处是否仍含合法 Unicode。

## 2. 正则 `SyntaxError: Invalid regular expression: /??/g`

**根因**：源码中本意是去掉 emoji **`⚠️`**，写作 `s.replace(/⚠️/g, '')`。编码一旦损坏，`⚠️` 变成两个 **`?`**，源码变成 `replace(/??/g, ...)`，**`/??/` 不是合法正则**（第二个 `?` 被解析为量词），页面脚本直接崩溃。

**规则**：

- 若需过滤警告符号，保持 **`/⚠️/g`**（或改用 `\uXXXX` 转义），**禁止**在不确定编码的情况下手写易被破坏的多字节字面量；绝不允许出现 **`/??/`** 形式的正则字面量。
- 损坏发生后：**先恢复 UTF-8 源文件**，再谈功能，不要靠「改成 `/\\?/g`」掩盖乱码根因。

## 3. 折叠面板「吸顶」：默认只改 CSS

**事故**：为吸顶改 JS，扩大破坏面且非必要。

**规则**：

- 折叠标题吸顶：先用 **`position: sticky`**、限定在**右栏容器**（如 `#workspace-pane`）内，并注意父级 **`overflow`** 是否裁剪 sticky。
- **不要**为吸顶引入或改写 `safeMarkedParse`、marked 渲染链，除非有明确产品需求。

## 4. 自检清单（改完 `index.html` 必做）

- [ ] `git diff` 体量合理（仅预期修改，而非整文件替换）。
- [ ] 控制台无 `Invalid regular expression`。
- [ ] 随机抽几处中文注释/界面文案，非 `???`。
