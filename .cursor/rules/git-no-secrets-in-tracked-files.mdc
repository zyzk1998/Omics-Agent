---
description: 推送纪律——跟踪代码中禁止真实内网 IP、密钥与可识别生产端口；机密走本地覆盖文件
alwaysApply: true
---

# Git 推送与跟踪文件：禁止真实 IP / 密钥 / 环境绑定端口

本规则为项目**长记忆**：凡修改、新增或审查将纳入 Git **已跟踪**路径的代码、配置与文档时，须保证**后续 push 中不再出现**下列内容（测试占位除外）。

## 1. 禁止进入已跟踪文件的内容

- **真实内网 / 非公网地址**：如 `192.168.x.x`、`10.x.x.x`、`172.16–31.x.x` 等用于实际机房的地址；仓库内默认值一律使用 **`127.0.0.1`** 或 **`localhost`**，或通过**环境变量**读取且无敏感默认。
- **密钥与令牌**：任何云厂商 API Key（如 `sk-` 前缀长串）、JWT、密码、私钥 PEM 内容；仅允许出现**占位符**（如 `your_api_key_here`）或**从环境变量读取**的代码，不得提交可激活的明文。
- **与特定部署绑定的端口**：不得在跟踪文件中写死**仅某台内网机器**使用的「地址+端口」组合作为唯一默认；默认应为本机占位（`127.0.0.1:端口`）或占位主机 + 文档说明由 `HPC_MCP_URL`、`OMICS_AGENT_WEB_URL` 等注入。

## 2. 机密与真实拓扑应放何处（已在 `.gitignore` 或应使用）

- **`.env`**、**`.env.*.local`**、**`.gibh_local.env`**
- **`docker-compose.override.yml`**、**`docker-compose.local.yml`**
- **`gibh-desktop-app/config.local.json`**
- **`config/settings.local.yaml`**（若存在）

**注意**：`.gitignore` 只对**未跟踪**文件生效；已跟踪文件一旦写入敏感信息，仍会随 commit 进入历史。禁止依赖「把 `server.py` 加进 `.gitignore`」代替脱敏。

## 3. 变更前自检（建议）

在提交或推送前，对将提交的范围执行检索，确认无：

- `192.168.`、`10.`、`172.1`、`172.2`、`172.3`（按需扩展）中非文档性用途
- `sk-[a-zA-Z0-9]{20,}` 等疑似 key 形态
- 已知的固定生产 URL 与明文密钥对

## 4. 与本仓库现状对齐

- **HPC MCP / 桌面 Web 基址**：默认应指向 `127.0.0.1`；真实 `192.168.x.x` 仅允许出现在上述**被忽略**的本地覆盖文件或部署环境变量中。
- **硅基流动等 LLM**：仅 `SILICONFLOW_API_KEY` 等**变量名**与 `getenv` 逻辑可存在于跟踪代码；**值**只存在于 `.env`（已忽略）。

若产品要求必须在仓库中保留「示例 URL」，须为**明显虚构**或 **example.com**，且不得与现网一致。
