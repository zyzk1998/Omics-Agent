---
description: 交付说明格式——代码改动与精炼总结分节，避免挤占任务上下文
alwaysApply: true
---

# 交付说明书写标准

在完成需求后的回复中，**必须拆成两个独立部分**（需求场景不同：有时要看改动细节，有时只要结论）。

## 1. 代码改动（或「具体改动」）

- 列出**改动的文件路径**及**每处做了什么**（可配合必要时的代码引用）。
- 本段面向「要对照仓库、review、继续改」的场景，**可写细**。

## 2. 精炼总结

- **单独成节**，与上一节明确分隔（用二级标题即可）。
- **短**：几句话说清目的、行为变化、你需要注意的一点即可。
- **禁止**把总结写成长篇复述，以免挤占后续正式任务的上下文。

两节顺序固定：**先具体改动，后精炼总结**（若用户只要其一，可在总结首句说明「本节仅总结 / 本节仅列改动」）。

## 3. 运维与操作说明（必选收尾）

- 在**精炼总结末尾另起一条**（单独小节或单独列表项均可），简要说明与本需求相关的**运维动作**：是否需要重启容器 / API / 进程、建议执行的 CLI、或明确写「无需特别操作」及仍须留意的事项（配置、缓存、数据路径等）。
- 避免默认省略；若确实无操作要求，也应写一句「无需重启，但请注意 …」占位，便于排障与交接。
- **可执行指令纪律（与仓库《底层算子融合智能体系统标准方法论》§5.5 一致）**：凡运维说明涉及**需在本机或服务器上执行的操作**（重启、重建镜像、补种数据、清理缓存等），**必须粘贴完整、可复制执行的命令**（含假定工作目录；`docker compose` / `kubectl` / `systemctl` 等写全子命令与**典型服务名**）。禁止仅用「重启 worker」「更新一下」等无对象的叙述代替**怎么干**。若路径或服务名因环境而异，在命令同一行或紧邻注释中写明**替换占位规则**后仍保持一条可运行示例。确无 CLI 时写清「无需执行命令」。
