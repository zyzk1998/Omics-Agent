---
description: 交付说明格式——代码改动与精炼总结分节，避免挤占任务上下文
alwaysApply: true
---

# 交付说明书写标准

在完成需求后的回复中，**必须拆成两个独立部分**（需求场景不同：有时要看改动细节，有时只要结论）。

## 1. 代码改动（或「具体改动」）

- 列出**改动的文件路径**及**每处做了什么**（可配合必要时的代码引用）。
- 本段面向「要对照仓库、review、继续改」的场景，**可写细**。

## 2. 精炼总结

- **单独成节**，与上一节明确分隔（用二级标题即可）。
- **短**：几句话说清目的、行为变化、你需要注意的一点即可。
- **禁止**把总结写成长篇复述，以免挤占后续正式任务的上下文。

两节顺序固定：**先具体改动，后精炼总结**（若用户只要其一，可在总结首句说明「本节仅总结 / 本节仅列改动」）。

## 3. 运维与操作说明（必选收尾）

- 在**精炼总结之后**单独开辟一节，**必须使用以下三级标题之一作为小节标题**（便于检索与交接）：
  - **`### 🛠️ 运维与操作说明 (DevOps Guide)`**（推荐，涉及 Docker/镜像/进程/缓存等任一类时优先使用）
  - 若仅需一句话带过，仍须在文末保留浅显运维提示，但标题可省略 emoji，内容不得省略可执行动作。
- 在该小节中说明与本需求相关的**运维动作**：是否需要重建镜像、重启容器 / API / Node / Python 进程、或**强制刷新浏览器缓存**；若仅需前端静态生效，须写明 **Ctrl+Shift+R（硬刷新）** 或等效操作。
- **直白纪律**：禁止「重启一下」「更新缓存」等无对象表述；须写清**对谁**执行**什么**。凡涉及宿主机命令，**必须粘贴完整、可复制执行的一行或多行 Shell**（含假定工作目录；`docker compose` / `kubectl` / `systemctl` 写全子命令与**典型服务名**）。若服务名因环境而异，在同一行或紧邻注释写明占位替换规则后仍给出可运行示例。
- 若确实无需任何 CLI（例如纯静态 CSS 且无需清 CDN），仍须在该小节写一句结论（如「无需重启后端；请强制刷新浏览器」），避免默认省略。
- **可执行指令纪律（与仓库《底层算子融合智能体系统标准方法论》§5.5 一致）**：确无 CLI 时写清「无需执行命令」及仍须留意的事项（配置、缓存、数据路径等）。
